//STEP 1
// let userSelection = parseInt(prompt("Please select any number."));
// console.log(`The positive version of the number ${userSelection} is ${Math.abs(userSelection)}.`);

//STEP 2
// let userSelection = parseFloat(prompt("Please select any floating point value (decimal)."));
// console.log(`The decimal ${userSelection} rounded to the nearest whole number is ${Math.ceil(userSelection)}.`);

//STEP 3
// let userSelection = parseFloat(prompt("Please select any floating point value (decimal)."));
// console.log(`The decimal ${userSelection} rounded down to the nearest whole number is ${Math.floor(userSelection)}.`);

//STEP 4
// let userSelection = prompt("Please enter a list of 5 (FIVE) comma delimited numbers, ie '1,2,3,4,5'.");
// let splitSelection = userSelection.split(',');
// let max = parseInt(splitSelection[0]);
// let min = parseInt(splitSelection[0]);
// splitSelection.forEach((x) => {
//     max = Math.max(max, x);
//     min = Math.min(min, x);
// });
// console.log(`The largest inputted number is ${max} and the smallest number inputted is ${min}.`);

//STEP 5
// let userSelection = parseInt(prompt("Please select a number."));
// console.log(`The square root of ${userSelection} calculates out to be ${Math.sqrt(userSelection)}`);
